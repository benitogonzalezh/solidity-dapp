version: "3.8"

services:
  dapp:
    container_name: dapp
    build: .
    image: dapp
    volumes:
      - .:/dapp/
    ports:
      - "3000:3000"
    command: tail -F anything
  ganache:
    container_name: ganache
    image: trufflesuite/ganache-cli:latest
    ports:
      - "8545:8545"